void find(int *frame,int *block, int *x, int *y)
{
    int blockRow=16,frameRow=256, blockLength=16*16,frameLength=256*256;

    for(int i=0;i<frameRow-blockRow;i++)
    {
        int sum=0;
        int Offset=frameLength/frameRow;
        for(int j=0;j<blockRow;j++)
        {
                
        }
    }
}